## 1. Entry List View — Back to feed list

- [x] 1.1 Add `leading=ft.IconButton(ft.Icons.ARROW_BACK, on_click=...)` to AppBar that pushes `/feeds`

## 2. Entry View — Back to entry list

- [x] 2.1 Add `leading=ft.IconButton(ft.Icons.ARROW_BACK, on_click=...)` to AppBar that pushes `/feed/<entry.feed>`

## 3. Verify

- [x] 3.1 Run `ruff check .` and `uv run mypy .`

## 1. Create `.env.sample`

- [x] 1.1 Create `.env.sample` at project root with all `COUSCOUS_DATABASE_*` variables documented, commented out, with descriptions and defaults
- [x] 1.2 Verify `.env` is already in `.gitignore` (confirmed — no action needed)
- [x] 1.3 Run `ruff check .` and `ruff format .` on the new file

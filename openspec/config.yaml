schema: meu-fluxo-qa

context: |
    Você está trabalhando em um aplicativo de leitura de notícias.
    Tecnologias principais:
    - Python Flet (wrapper Flutter)
    - Banco de Dados: PostgreSQL, SQLModel (Async), Alembic para migrations.
    - Infraestrutura: Docker e Docker Compose, gerenciador de pacotes 'uv', linter/formatter 'Ruff'.

    Diretriz crítica:
    - Todas as comunicações e documentação interna devem ser feitas em Português do Brasil (PT-BR).

rules:
  proposal:
    - "Após gerar proposal.md, execute a skill grill-me para refinar o artefato."

  design:
    - "Após gerar design.md, execute a skill grill-me para refinar o artefato."

  specs:
    - "Após gerar specs/, execute a skill grill-me para refinar os artefatos."

  qa-plan:
    - "Após gerar qa-plan.md, execute a skill grill-me para refinar o artefato."

  tasks:
    - "Após gerar tasks.md, execute a skill grill-me para refinar o checklist."
